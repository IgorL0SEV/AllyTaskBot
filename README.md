# AllyTaskBot 🤖

Telegram-бот для команды: собирает идеи и задачи в одном месте, хранит их в SQLite и не даёт утонуть в переписке чата.

Каждый участник может добавить задачу (с категорией и статусом), в любой момент посмотреть список всех задач или выгрузить их в CSV для Excel/LibreOffice.

## Возможности

- ➕ **Добавление задач** через FSM-сценарий: текст → категория → статус (inline-кнопки).
- 📋 **Просмотр всех задач** одной командой — с автором, категорией, статусом и датой.
- 📄 **Экспорт в CSV** — файл открывается в Excel/LibreOffice/Google Sheets (UTF-8 с BOM, кириллица читается корректно).
- 👥 **Поддержка множественных пользователей** — каждая задача помечается автором (`@username` или имя).
- 💾 **Автоматическое сохранение в SQLite** — данные не теряются при перезапуске или сбое.
- 🗂 **Категории и статусы** — «новое / в работе / выполнено» и «фронт-энд / бэк-энд / база данных / общее».

## Архитектура

Проект построен на трёх частях (аналогия из ТЗ — ресторан):

| Часть | Роль в проекте | Аналогия |
|-------|----------------|----------|
| **Интерфейс** | Telegram — окно диалога, кнопки, сообщения | Меню ресторана |
| **Логика** | Python + aiogram — обработка команд, FSM, БД | Кухня |
| **Данные** | SQLite3 — файл `data/tasks.db` | Склад с ингредиентами |

Поток: пользователь пишет команду в Telegram → aiogram-роутер принимает её → логика пишет/читает SQLite → ответ уходит обратно в Telegram.

## Состав проекта

```
AllyTaskBot/
├── main.py              # точка входа: запуск polling
├── config.py            # загрузка BOT_TOKEN / DB_PATH из .env
├── requirements.txt     # зависимости
├── .env.example         # шаблон переменных окружения
├── .gitignore
├── README.md
├── database/
│   ├── __init__.py
│   └── db.py            # класс Database (схема, add, get_all, export_csv)
├── handlers/
│   ├── __init__.py      # register_handlers(dp) — сборка роутеров
│   ├── start.py         # /start
│   ├── add.py           # /add + FSM (text → category → status)
│   ├── list.py          # /list
│   ├── list_csv.py      # /list_csv — отправка CSV-файла
│   └── keyboards.py     # inline-клавиатуры category / status
└── data/
    └── tasks.db         # база SQLite (создаётся автоматически, в .gitignore)
```

## Схема базы данных

Таблица `tasks`:

| Колонка     | Тип       | Описание |
|-------------|-----------|----------|
| `id`        | INTEGER PK | Уникальный идентификатор (autoincrement) |
| `text`      | TEXT      | Текст задачи / идеи |
| `user`      | TEXT      | Автор задачи (`@username` или имя) |
| `created_at`| TIMESTAMP | Дата и время создания |
| `status`    | TEXT      | Статус: «новое», «в работе», «выполнено» |
| `category`  | TEXT      | Категория: «фронт-энд», «бэк-энд», «база данных», «общее» |

## Установка и запуск

### 1. Клонировать репозиторий

```bash
git clone https://github.com/IgorL0SEV/AllyTaskBot.git
cd AllyTaskBot
```

### 2. Создать виртуальное окружение и установить зависимости

```bash
python -m venv .venv
# Windows (PowerShell):
.venv\Scripts\Activate.ps1
# Linux / macOS:
source .venv/bin/activate

pip install -r requirements.txt
```

> Требуется Python 3.8+ (рекомендуется 3.11+).

### 3. Получить токен бота

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram.
2. Отправьте `/newbot`, задайте имя и username.
3. Скопируйте выданный токен вида `123456789:ABC...`.

### 4. Настроить `.env`

```bash
cp .env.example .env
```

Откройте `.env` и впишите токен:

```
BOT_TOKEN=123456789:ABCyourtokenhere
DB_PATH=data/tasks.db
```

### 5. Запустить бота

```bash
python main.py
```

В консоли появится `Бот запущен. Polling started...`. Откройте бота в Telegram и отправьте `/start`.

## Команды бота

| Команда     | Действие |
|-------------|----------|
| `/start`    | Приветствие и список команд. |
| `/add`      | Добавить задачу: ввод текста → выбор категории → выбор статуса → сохранение. |
| `/list`     | Показать все задачи (с автором, категорией, статусом, датой). |
| `/list_csv` | Выгрузить все задачи в CSV-файл и отправить документом. |

## Дальнейшее развитие (масштабирование)

MVP готов к расширению:

- 📌 Назначение ответственного за задачу (колонка `assignee`, выбор из участников).
- 🗂 Группировка по категориям и фильтры (`/list category:backend`).
- 🧵 Использование топиков (forum-чат) для тематического разделения.
- 🔒 Авторизация: ограничение доступа по `user_id` / `username` / ключу, чтобы посторонние не добавляли задачи.
- 📊 Интеграция с Excel/Google Sheets через API для расширенной аналитики.
- ⚡ Переход с синхронного `sqlite3` на `aiosqlite` (или PostgreSQL) при росте нагрузки.
- 🔄 Команды смены статуса/категории (`/status`, `/category`) для уже добавленных задач.

## Стек

- [Python 3](https://www.python.org/) — язык.
- [aiogram 3.x](https://docs.aiogram.dev/) — async-фреймворк для Telegram-ботов.
- [SQLite3](https://docs.python.org/3/library/sqlite3.html) — встроенная БД.
- [python-dotenv](https://github.com/theskumar/python-dotenv) — загрузка `.env`.

## Лицензия

Учебный проект, свободен к использованию и доработке.